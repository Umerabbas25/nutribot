const readline = require('readline'); const rl = readline.createInterface({ input: process.stdin, output: process.stdout }); console.log('\n============================================='); console.log(' NutriBot Direct Chat Simulator'); console.log('============================================='); console.log('Because Meta is dropping your inbound WhatsApp messages,'); console.log('you can type your messages here. The bot will receive them'); console.log('and send its replies DIRECTLY to your real WhatsApp app!'); console.log('Type your message below and press Enter (or type exit to quit):\n'); const ask = () => { rl.question('You: ', async (text) => { if(text.toLowerCase() === 'exit') return rl.close(); try { await fetch('http://localhost:3000/whatsapp/webhook', { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ object: 'whatsapp_business_account', entry: [{ id: '123', changes: [{ value: { messaging_product: 'whatsapp', metadata: { display_phone_number: '15551995221', phone_number_id: '1172611692611974' }, contacts: [{ profile: { name: 'Umer' }, wa_id: '923354506534' }], messages: [{ from: '923354506534', id: 'fake_' + Date.now(), timestamp: String(Math.floor(Date.now() / 1000)), text: { body: text }, type: 'text' }] }, field: 'messages' }] }] }) }); console.log('--> Message sent to bot! Check your WhatsApp for the reply.'); } catch(e) { console.error('Error:', e.message); } ask(); }); }; ask();
